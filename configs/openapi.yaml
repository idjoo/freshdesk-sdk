---
# Source: openapi/templates/openapi.yaml
# DON'T MODIFY, this openapi spec is generated by helm

openapi: 3.1.0
info:
  title: ID - Smartfren - CCAI API
  description: ""
  contact:
    email: adrianus.vian.habirowo@devoteam.com
  version: v2
basePath: /api/v2
schemes:
    - https
servers:
  - url: example.freshdesk.com
tags:
  - name: Tickets
    description: A ticket is an issue raised by a requester that need to be solved. It could be an urgent, high-priority problem exposing a security vulnerability. It could also be low priority question about a free T-shirt. Tickets are assigned to agents based on the agent's expertise and on the subject of the ticket.
paths:
  /tickets:
    post:
      operationId: create_ticket
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTicketRequest'
        description: ""
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Ticket'
          description: Successful operation
      tags:
      - Tickets
components:
  schemas:
    Conversations:
      properties:
        attachments:
          description: Ticket attachments. The total size of these attachments cannot
            exceed 20MB.
          items:
            format: binary
            type: string
          type: array
        body:
          description: Content of the conversation in HTML.
          type: string
        body_text:
          description: Content of the conversation in plain text.
          type: string
        created_at:
          description: Conversation creation timestamp.
          format: date-time
          type: string
        id:
          description: ID of the conversation.
          type: number
        incoming:
          description: Set to true if a particular conversation should appear as being
            created from outside (i.e., not through web portal).
          type: boolean
        last_edited_at:
          description: Timestamp when the conversation last edited.
          format: date-time
          type: string
        last_edited_user_id:
          description: ID of the agent who has last edited the conversation.
          format: date-time
          type: string
        private:
          description: Set to true if the note is private.
          type: boolean
        source:
          description: Denotes the type of the conversation.
          type: number
        support_email:
          description: Email address from which the reply is sent. For notes, this value
            will be null.
          type: string
        ticket_id:
          description: ID of the ticket to which this conversation is being added.
          type: number
        to_emails:
          description: Email addresses of agents/users who need to be notified about this
            conversation.
          items:
            type: string
          type: array
        updated_at:
          description: Conversation updated timestamp.
          format: date-time
          type: string
        user_id:
          description: ID of the agent/user who is adding the conversation.
          type: number
      type: object
    CreateReplyRequests:
      properties:
        attachments:
          description: Ticket attachments. The total size of these attachments cannot
            exceed 20MB.
          items:
            format: binary
            type: string
          type: array
        bcc_emails:
          description: Email address added in the 'bcc' field of the outgoing ticket email.
          type: string
        body:
          description: Content of the note in HTML format.
          type: string
        cc_emails:
          description: Email address added in the 'cc' field of the outgoing ticket email.
          type: string
        from_email:
          description: The email address from which the reply is sent. By default the
            global support email will be used.
          type: string
        user_id:
          description: ID of the agent who is adding the note.
          type: string
      required:
      - body
      type: object
    CreateTicketRequest:
      allOf:
      - $ref: '#/components/schemas/TicketBase'
      properties:
        internal_agent_id:
          description: ID of the internal agent which the ticket should be assigned with.
          type: integer
        internal_group_id:
          description: ID of the internal group to which the ticket should be assigned
            with.
          type: integer
        lookup_parameter:
          description: This attribute for tickets can only be set if Custom Objects is
            enabled and a lookup field has been added under ticket fields.The value can
            either be in the form of the display_id (record id) or primary_field_value
            (user defined record value). The default value is display_id.
          type: string
        parent_id:
          description: ID of the parent ticket that this ticket should be linked to. When
            passing this field, the current ticket actioned upon will be converted to
            a child ticket.
          type: number
        priority:
          default: 1
          description: Priority of the ticket. The default value is 1.
          type: number
        source:
          default: 2
          description: The channel through which the ticket was created. The default value
            is 2.
          type: number
        status:
          default: 2
          description: Status of the ticket. The default Value is 2.
          type: number
        subject:
          default: null
          description: Subject of the ticket. The default Value is null.
          type: string
        type:
          default: null
          description: Helps categorize the ticket according to the different kinds of
            issues your support team deals with. The default Value is null.
          type: string
        unique_external_id:
          description: External ID of the requester. If no contact exists with this external
            ID in Freshdesk, they will be added as a new contact.
          type: string
      type: object
    Ticket:
      allOf:
      - $ref: '#/components/schemas/TicketBase'
    TicketBase:
      properties:
        attachments:
          description: Ticket attachments. The total size of these attachments cannot
            exceed 20MB.
          items:
            format: binary
            type: string
          type: array
        cc_emails:
          description: Email address added in the 'cc' field of the incoming ticket email.
          items:
            type: string
          type: array
        company_id:
          description: Company ID of the requester. This attribute can only be set if
            the Multiple Companies feature is enabled (Estate plan and above).
          type: number
        created_at:
          description: Ticket creation timestamp.
          format: date-time
          type: string
        deleted:
          description: Set to true if the ticket has been deleted/trashed. Deleted tickets
            will not be displayed in any views except the "deleted" filter.
          type: boolean
        description:
          description: HTML content of the ticket.
          type: string
        description_text:
          description: Content of the ticket in plain text.
          type: string
        due_by:
          description: Timestamp that denotes when the ticket is due to be resolved.
          format: date-time
          type: string
        email:
          description: Email address of the requester. If no contact exists with this
            email address in Freshdesk, it will be added as a new contact.
          format: email
          type: string
        email_config_id:
          description: ID of email config which is used for this ticket. (i.e., support@yourcompany.com/sales@yourcompany.com).
          type: number
        facebook_id:
          description: Facebook ID of the requester. A contact should exist with this
            facebook_id in Freshdesk.
          type: string
        fr_due_by:
          description: Timestamp that denotes when the first response is due.
          format: date-time
          type: string
        fr_escalated:
          description: Set to true if the ticket has been escalated as the result of first
            response time being breached.
          type: boolean
        fwd_emails:
          description: Email address(e)s added while forwarding a ticket.
          items:
            type: string
          type: array
        group_id:
          description: ID of the group to which the ticket has been assigned.
          type: number
        id:
          description: Unique ID of the ticket.
          type: number
        is_escalated:
          description: Set to true if the ticket has been escalated for any reason.
          type: boolean
        name:
          description: Name of the requester.
          type: string
        phone:
          description: Phone number of the requester. If no contact exists with this phone
            number in Freshdesk, it will be added as a new contact. If the phone number
            is set and the email address is not, then the name attribute is mandatory.
          type: string
        priority:
          description: Priority of the ticket.
          type: number
        product_id:
          description: ID of the product to which the ticket is associated. It will be
            ignored if the email_config_id attribute is set in the request.
          type: number
        reply_cc_emails:
          description: Email address added while replying to a ticket.
          items:
            type: string
          type: array
        requester_id:
          description: User ID of the requester. For existing contacts, the requester_id
            can be passed instead of the requester's email.
          type: string
        responder_id:
          description: ID of the agent to whom the ticket has been assigned.
          type: number
        source:
          description: The channel through which the ticket was created.
          type: number
        spam:
          description: Set to true if the ticket has been marked as spam.
          type: boolean
        status:
          description: Status of the ticket.
          type: number
        subject:
          description: Subject of the ticket.
          type: string
        tags:
          description: Tags that have been associated with the ticket.
          items:
            type: string
          type: array
        to_emails:
          description: Email addresses to which the ticket was originally sent.
          items:
            type: string
          type: array
        twitter_id:
          description: Twitter handle of the requester. If no contact exists with this
            handle in Freshdesk, it will be added as a new contact.
          type: string
        type:
          description: Helps categorize the ticket according to the different kinds of
            issues your support team deals with.
          type: string
        updated_at:
          description: Ticket updated timestamp.
          format: date-time
          type: string
      type: object
securitySchemes:
  basicAuth:
    type: http
    scheme: basic
security:
  - basicAuth: []
